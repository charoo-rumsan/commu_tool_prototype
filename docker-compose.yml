version: "3.9"
services:
  app:
    build: .
    container_name: qdrant_header_service
    ports:
      - "8000:8000"
    depends_on:
      - qdrant
    volumes:
      - ./app:/app
  qdrant:
    image: qdrant/qdrant:v1.7.3
    container_name: qdrant_db
    ports:
      - "6333:6333"
